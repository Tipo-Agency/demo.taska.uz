# Критические исправления Telegram бота - 2026-01-24 (v3)

## Выполненные исправления

### 1. ✅ ИСПРАВЛЕНЫ ВСЕ КНОПКИ В НАСТРОЙКАХ УВЕДОМЛЕНИЙ

**Проблема:** Все кнопки в настройках уведомлений не работали (не только комментарий).

**Причина:** Использовалось `startswith` для определения категории, что вызывало конфликты.

**Исправления:**
- Использовано точное сравнение названий настроек вместо `startswith`
- Для каждой категории используется список точных названий
- Добавлена поддержка групповых уведомлений (`groupDailySummary`, `groupSuccessfulDeals`)

**Файлы:**
- `telegram-bot/bot.py` - функция `settings_toggle_notification`

**Изменения:**
```python
# Было:
if setting_name.startswith('newTask') or setting_name.startswith('statusChange') or setting_name.startswith('task'):

# Стало:
if setting_name in ['newTask', 'statusChange', 'taskComment']:
elif setting_name in ['docCreated', 'docUpdated', 'docShared']:
elif setting_name in ['meetingCreated', 'meetingReminder', 'meetingUpdated']:
# и т.д.
```

---

### 2. ✅ ДОБАВЛЕНЫ НАСТРОЙКИ ГРУППОВЫХ УВЕДОМЛЕНИЙ

**Проблема:** Не было настроек для включения/выключения групповых уведомлений.

**Исправления:**
- Добавлены настройки в `settings_notif_group`:
  - 📋 Ежедневная сводка (9:00) - включить/выключить
  - 🎉 Успешные сделки - включить/выключить
- Настройки сохраняются в `notificationPrefs`:
  - `groupDailySummary.telegramGroup`
  - `groupSuccessfulDeals.telegramGroup`
- По умолчанию обе настройки включены (`telegramGroup: true`)

**Файлы:**
- `telegram-bot/bot.py` - функция `settings_notif_group` и `settings_toggle_notification`
- `telegram-bot/scheduler.py` - функция `send_group_daily_summary`
- `telegram-bot/bot.py` - функция `periodic_check`

---

### 3. ✅ ПРОВЕРКА НАСТРОЕК В ПЛАНИРОВЩИКЕ И ПЕРИОДИЧЕСКОЙ ПРОВЕРКЕ

**Исправления:**
- В `send_group_daily_summary` добавлена проверка `groupDailySummary.telegramGroup`
- В `periodic_check` добавлена проверка `groupSuccessfulDeals.telegramGroup`
- Если настройка выключена, уведомление не отправляется

**Файлы:**
- `telegram-bot/scheduler.py` - функция `send_group_daily_summary`
- `telegram-bot/bot.py` - функция `periodic_check`

---

### 4. ✅ СОХРАНЕНИЕ telegramUserId ПРИ АВТОРИЗАЦИИ

**Важно:** При авторизации в боте автоматически сохраняется `telegramUserId` в профиле пользователя.

**Код:**
```python
# В функции password после успешной авторизации:
user['telegramUserId'] = str(telegram_user_id)
firebase.save('users', user)
```

**Как это работает:**
1. Пользователь авторизуется в боте
2. `telegramUserId` сохраняется в профиле пользователя
3. Веб-приложение использует `telegramUserId` для отправки уведомлений через `services/notificationService.ts`
4. Уведомления отправляются в личный чат пользователя в Telegram

**Файлы:**
- `telegram-bot/bot.py` - функция `password`

---

### 5. ✅ ГРУППОВЫЕ УВЕДОМЛЕНИЯ

**Два типа групповых уведомлений:**

1. **Ежедневная сводка (9:05)**
   - Время: 9:05 (Asia/Tashkent)
   - Содержание:
     - Задачи на вчера (не выполненные)
     - Ранее просроченные задачи
     - Задачи на сегодня с ответственными (с группировкой по сотрудникам)
   - Настройка: `groupDailySummary.telegramGroup`
   - По умолчанию: включено

2. **Успешные сделки**
   - Триггер: Когда сделка переходит в стадию "won"
   - Формат: "🎉 Всем привет, поздравляю! У нас новый клиент!" + детали сделки
   - Настройка: `groupSuccessfulDeals.telegramGroup`
   - По умолчанию: включено

**Файлы:**
- `telegram-bot/scheduler.py` - ежедневная сводка
- `telegram-bot/bot.py` - успешные сделки в `periodic_check`

---

## Важно про уведомления

**Как работают уведомления:**

1. **Пользователь авторизуется в боте** → `telegramUserId` сохраняется в профиле
2. **Веб-приложение создает задачу/сделку** → вызывает `notifyTaskCreated` / `notifyDealCreated`
3. **Уведомление отправляется** через `services/telegramService.ts` используя `telegramUserId` из профиля
4. **Пользователь получает уведомление** в личный чат в Telegram

**Настройки уведомлений:**
- Пользователь может настроить уведомления в боте (включить/выключить)
- Настройки сохраняются в `notificationPrefs` в Firebase
- Веб-приложение проверяет настройки перед отправкой уведомлений

---

## Тестирование

После исправлений необходимо проверить:

1. ✅ **Все кнопки в настройках** - работают корректно
2. ✅ **Настройки групповых уведомлений** - можно включить/выключить
3. ✅ **Ежедневная сводка** - отправляется только если включена в настройках
4. ✅ **Успешные сделки** - отправляются только если включены в настройках
5. ✅ **telegramUserId** - сохраняется при авторизации
6. ✅ **Уведомления** - приходят в личный чат пользователя

---

## Файлы, измененные в этом обновлении

1. `telegram-bot/bot.py` - исправлены все кнопки, добавлены настройки групповых уведомлений
2. `telegram-bot/scheduler.py` - добавлена проверка настроек для ежедневной сводки
3. `docs/tz/TELEGRAM_BOT_TZ.md` - обновлено ТЗ
